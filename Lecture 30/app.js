export const a = 10;

export const arr = [1, 2, 3, 4, 5];

export const obj = { name: "Aman", age: 24 };

export const sum = () => {
  console.log("addition fun...");
};

const str = "lorem ipsum";
export default str;

// array - collection of multiple values store based on index.
// object - collection of multiple values in key-value pair

// 5 words  -> sequence

// types of export
// 1. named export
// 2. default export - limit(minimum one default export allow in each file)
